// 7 variable find MAximum Number using Nested If -else

#include <stdio.h>
int main()
{
    int a, b, c, d, e, f, g;

    printf(" Enter The Value of First Number a \t :");
    scanf("%d", &a);
    printf(" Enter The Value of Second Number b \t :");
    scanf("%d", &b);
    printf(" Enter The Value of Third Number c \t :");
    scanf("%d", &c);
    printf(" Enter The Value of Fourth Number d \t :");
    scanf("%d", &d);
    printf(" Enter The Value of Fifth Number e \t :");
    scanf("%d", &e);
    printf(" Enter The Value of Sixth Number f \t :");
    scanf("%d", &f);
    printf(" Enter The Value of Seventh Number g \t :");
    scanf("%d", &g);

    if (a > b)
    {
        if (a > c)
        {
            if (a > d)
            {
                if (a > e)
                {
                    if (a > f)
                    {
                        if (a > g)
                        {
                            printf(" The Maximum Number is \t : %d", a);
                        }
                        else
                        {
                            printf(" The Maximum Number is \t : %d", g);
                        }
                    }
                    else
                    {
                        if (f > g)
                        {
                            printf(" The Maximum Number is \t : %d", f);
                        }
                        else
                        {
                            printf(" The Maximum Number is \t : %d", g);
                        }
                    }
                }
                else
                {
                    if (e > f)
                    {
                        if (e > g)
                        {
                            printf(" The Maximum Number is \t : %d", e);
                        }
                        else
                        {
                            printf(" The Maximum Number is \t : %d", g);
                        }
                    }
                    else
                    {
                        if (f > g)
                        {
                            printf(" The Maximum Number is \t : %d", f);
                        }
                        else
                        {
                            printf(" The Maximum Number is \t : %d", g);
                        }
                    }
                }
            }
            else
            {
                if (d > e)
                {
                    if (d > f)
                    {
                        if (d > g)
                        {
                            printf(" The Maximum Number is \t : %d", d);
                        }
                        else
                        {
                            printf(" The Maximum Number is \t : %d", g);
                        }
                    }
                    else
                    {
                        if (f > g)
                        {
                            printf(" The Maximum Number is \t : %d", f);
                        }
                        else
                        {
                            printf(" The Maximum Number is \t : %d", g);
                        }
                    }
                }
                else
                {
                    if (e > f)
                    {
                        if (e > g)
                        {
                            printf(" The Maximum Number is \t : %d", e);
                        }
                        else
                        {
                            printf(" The Maximum Number is \t : %d", g);
                        }
                    }
                    else
                    {
                        if (f > g)
                        {
                            printf(" The Maximum Number is \t : %d", f);
                        }
                        else
                        {
                            printf(" The Maximum Number is \t : %d", g);
                        }
                    }
                }
            }
        }
        else
        {
            if (c > d)
            {
                if (c > e)
                {
                    if (c > f)
                    {
                        if (c > g)
                        {
                            printf(" The Maximum Number is \t : %d", c);
                        }
                        else
                        {
                            printf(" The Maximum Number is \t : %d", g);
                        }
                    }
                    else
                    {
                        if (f > g)
                        {
                            printf(" The Maximum Number is \t : %d", f);
                        }
                        else
                        {
                            printf(" The Maximum Number is \t : %d", g);
                        }
                    }
                }
                else
                {
                    if (e > f)
                    {
                        if (e > g)
                        {
                            printf(" The Maximum Number is \t : %d", e);
                        }
                        else
                        {
                            printf(" The Maximum Number is \t : %d", g);
                        }
                    }
                    else
                    {
                        if (f > g)
                        {
                            printf(" The Maximum Number is \t : %d", f);
                        }
                        else
                        {
                            printf(" The Maximum Number is \t : %d", g);
                        }
                    }
                }
            }
            else
            {
                if (d > e)
                {
                    if (d > f)
                    {
                        if (d > g)
                        {
                            printf(" The Maximum Number is \t : %d", d);
                        }
                        else
                        {
                            printf(" The Maximum Number is \t : %d", g);
                        }
                    }
                    else
                    {
                        if (f > g)
                        {
                            printf(" The Maximum Number is \t : %d", g);
                        }
                        else
                        {
                            printf(" The Maximum Number is \t : %d", g);
                        }
                    }
                }
                else
                {
                    if (e > f)
                    {
                        if (e > g)
                        {
                            printf(" The Maximum Number is \t : %d", e);
                        }
                        else
                        {
                            printf(" The Maximum Number is \t : %d", g);
                        }
                    }
                    else
                    {
                        if (f > g)
                        {
                            printf(" The Maximum Number is \t : %d", f);
                        }
                        else
                        {
                            printf(" The Maximum Number is \t : %d", g);
                        }
                    }
                }
            }
        }
    }
    else
    {
        if (b > c)
        {
            if (b > d)
            {
                if (b > e)
                {
                    if (b > f)
                    {
                        if (b > g)
                        {
                            printf(" The Maximum Number is \t : %d", b);
                        }
                        else
                        {
                            printf(" The Maximum Number is \t : %d", g);
                        }
                    }
                    else
                    {
                        if (f > g)
                        {
                            printf(" The Maximum Number is \t : %d", f);
                        }
                        else
                        {
                            printf(" The Maximum Number is \t : %d", g);
                        }
                    }
                }
                else
                {
                    if (e > f)
                    {
                        if (e > g)
                        {
                            printf(" The Maximum Number is \t : %d", e);
                        }
                        else
                        {
                            printf(" The Maximum Number is \t : %d", g);
                        }
                    }
                    else
                    {
                        if (f > g)
                        {
                            printf(" The Maximum Number is \t : %d", f);
                        }
                        else
                        {
                            printf(" The Maximum Number is \t : %d", g);
                        }
                    }
                }
            }
            else
            {
                if (d > e)
                {
                    if (d > f)
                    {
                        if (d > g)
                        {
                            printf(" The Maximum Number is \t : %d", d);
                        }
                        else
                        {
                            printf(" The Maximum Number is \t : %d", g);
                        }
                    }
                    else
                    {
                        if (f > g)
                        {
                            printf(" The Maximum Number is \t : %d", f);
                        }
                        else
                        {
                            printf(" The Maximum Number is \t : %d", g);
                        }
                    }
                }
                else
                {
                    if (e > f)
                    {
                        if (e > g)
                        {
                            printf(" The Maximum Number is \t : %d", e);
                        }
                        else
                        {
                            printf(" The Maximum Number is \t : %d", g);
                        }
                    }
                    else
                    {
                        if (f > g)
                        {
                            printf(" The Maximum Number is \t : %d", f);
                        }
                        printf(" The Maximum Number is \t : %d", g);
                    }
                }
            }
        }
        else
        {
            if (c > d)
            {
                if (c > e)
                {
                    if (c > f)
                    {
                        if (c > g)
                        {
                            printf(" The Maximum Number is \t : %d", c);
                        }
                        else
                        {
                            printf(" The Maximum Number is \t : %d", g);
                        }
                    }
                    else
                    {
                        if (f > g)
                        {
                            printf(" The Maximum Number is \t : %d", f);
                        }
                        else
                            printf(" The Maximum Number is \t : %d", g);
                    }
                }
                else
                {
                    if (e > f)
                    {
                        if (e > g)
                        {
                            printf(" The Maximum Number is \t : %d", e);
                        }
                        else
                        {
                            printf(" The Maximum Number is \t : %d", g);
                        }
                    }
                    else
                    {
                        if (f > g)
                        {
                            printf(" The Maximum Number is \t : %d", f);
                        }
                        else
                        {
                            printf(" The Maximum Number is \t : %d", g);
                        }
                    }
                }
            }
            else
            {
                if (d > e)
                {
                    if (d > f)
                    {
                        if (d > g)
                        {
                            printf(" The Maximum Number is \t : %d", d);
                        }
                        else
                        {
                            printf(" The Maximum Number is \t : %d", g);
                        }
                    }
                    else
                    {
                        if (f > g)
                        {
                            printf(" The Maximum Number is \t : %d", f);
                        }
                        else
                        {
                            printf(" The Maximum Number is \t : %d", g);
                        }
                    }
                }
                else
                {
                    if (e > f)
                    {
                        if (e > g)
                        {
                            printf(" The Maximum Number is \t : %d", e);
                        }
                        else
                        {
                            printf(" The Maximum Number is \t : %d", g);
                        }
                    }
                    else
                    {
                        if (f > g)
                        {
                            printf(" The Maximum Number is \t : %d", f);
                        }
                        else
                        {
                            printf(" The Maximum Number is \t : %d", g);
                        }
                    }
                }
            }
        }
    }
    return 0;
}