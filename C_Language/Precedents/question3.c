#include <stdio.h>
int main()
{

    int x = 5;
    int y = 3;
    int z = 2;
    printf("Result for Formula (x+y+z) whole Square is \t : %d", (x * x) + (y * y) + (z * z) + (2 * x * y) + (2 * y * z) + (2 * x * z));
    return 0;
}